node_modules/
dist/
build/
# The built client copied here for the compiled server to serve in
# production (see server/src/index.ts, scripts/update.sh,
# scripts/install.sh) — a build artifact, not source.
server/client/
.env
!.env.example
server/prisma/dev.db
server/prisma/dev.db-journal
server/uploads/
*.log
.DS_Store
